// TIPO NUMBER --------------------------------------------------------------------------------------------
const meuNumero = 3;
const primeiroNumero = 1;
const segundoNumero = 2;
const operacaoMatematica = primeiroNumero - segundoNumero;
//console.log(operacaoMatematica);

/*
é possível salvar em uma variável não somente o número em si, como em const num = 5;, mas também o resultado de uma operação (ou mais):
*/
const soma = 10 + 10;
const multiplicacao = 10 * 10;
const operacao = (soma + multiplicacao) + 10;
//utilizamos parênteses () para seguir a regra matemática de precedência nas operações.


// PONTO FLUTUANTE ----------------------------------------------------------------------------------------
const numeroPontoFlutuante = 3.3;
const pontoFlutuanteSemZero = .5;
const novaOperacao = primeiroNumero / numeroPontoFlutuante;
//console.log(novaOperacao);


// NaN -> Not a Number (não é um número) ------------------------------------------------------------------
const alura = 'Alura';
//console.log(alura * primeiroNumero)
