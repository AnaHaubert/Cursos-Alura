let nomes = ['ana Julia', 'Caio vinicius', 'BIA silva'];

const nomesAtualizados = nomes.map(nome => nome.toUpperCase()); //método toUpperCase(): deixa todas as letras maiúsculas.

console.log(nomesAtualizados);